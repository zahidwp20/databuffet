<% layout('layouts/boilerplate') -%>
<div class="overflow-hidden">
  <section
    class="bg-img py-5 d-flex flex-column justify-content-center"
    style="background-image: url('/images/bg/bg-about-2.png')"
  >
    <div class="container my-5 py-5">
      <div class="row">
        <div class="col-lg-6">
          <h1 class="text-info fw-lighter">Career Center</h1>
          <h1 class="display-4 fw-900 position-relative">
            Need some help breaking into data?
            <!-- <span
            class="rounded-1 d-block w-50 position-absolute end-0 translate-middle-x"
            style="height: 0.7rem; background: linear-gradient(-270deg, transparent 10%, var(--bs-info) 100%)"
            ></span> -->
          </h1>
          <p class="mt-3 fs-4">
            Schedule a 1:1 call with us to get personal advice on how to take your data career to the next level. Calls
            are 30 minutes & personalized to your profile, and scheduled at your convenience.
          </p>
        </div>
      </div>
    </div>
  </section>

  <section class="container">
    <div style="margin-top: -60px; width: 100%; height: 100%; overflow: scroll" id="my-cal-inline"></div>
  </section>
</div>
<!-- <section class="container fs-4 text-center py-5">
  <p>
    You can also reach us by sending us an email at
    <br />
    <a class="link-info text-decoration-none" href="#">support@onlinevideocourse.com</a>
  </p>
  <p>
    or calling us at
    <br />
    <a class="link-info text-decoration-none" href="#">+1-800-123-4567.</a>
  </p>
</section> -->

<!-- Cal inline embed code begins -->
<script type="text/javascript">
  (function (C, A, L) {
    let p = function (a, ar) {
      a.q.push(ar);
    };
    let d = C.document;
    C.Cal =
      C.Cal ||
      function () {
        let cal = C.Cal;
        let ar = arguments;
        if (!cal.loaded) {
          cal.ns = {};
          cal.q = cal.q || [];
          d.head.appendChild(d.createElement("script")).src = A;
          cal.loaded = true;
        }
        if (ar[0] === L) {
          const api = function () {
            p(api, arguments);
          };
          const namespace = ar[1];
          api.q = api.q || [];
          typeof namespace === "string" ? (cal.ns[namespace] = api) && p(api, ar) : p(cal, ar);
          return;
        }
        p(cal, ar);
      };
  })(window, "https://app.cal.com/embed/embed.js", "init");
  Cal("init", { origin: "https://cal.com" });

  Cal("inline", {
    elementOrSelector: "#my-cal-inline",
    calLink: "databuffet/30min",
    layout: "month_view",
  });

  Cal("ui", {
    theme: "dark",
    styles: { branding: { brandColor: "#9538F2" } },
    hideEventTypeDetails: false,
    layout: "month_view",
  });
</script>
<!-- Cal inline embed code ends -->
